# PA 2-1 实验报告



## 1. 思考题

### 1-1 使用`hexdump`命令查看测试用例的`.img`文件，所显示的`.img`文件的内容对应模拟内存的哪一个部分？指令在机器中表示的形式是什么？

### 1-2 如果去掉`instr_execute_2op()`函数前面的`static`关键字会发生什么情况？为什么？

### 1-3 为什么`test-float`会`fail`？以后在写和浮点数相关的程序的时候要注意什么？



## 2. 实验心得

这次PA在国庆期间完成，占掉将近一半的假期时间。其中阅读教程和理解框架代码大概用去两天（毕竟没学过汇编，一切从零开始），剩下两天都用来实现CPU功能。写代码和debug用去的时间可谓__二八开__，因此在10.6晚终于找出最后一个bug、看到一排齐刷刷的`GOOD`后，我决定在实验报告里记录下过去几天的宝贵经验，这样自己只有4个字节存储空间的脑袋就不会忘掉，我在PA2-1做过哪些事情、抖过什么机灵。

### 2-1 框架代码阅读

这件事情刚开始我是结合着读教程、看课本一起来做的。

在看到调试器功能时，我发现了一点小问题：如果只输入`info`，后面不接参数，调试器会产生段错误并直接退出。这个不寻常的情景意味着，程序里可能有一个小bug，无伤大雅但影响使用体验。那要怎么修复它呢？

我决定从`main.c`开始读框架代码。这是一个痛苦的过程：每个单词我都不陌生，可它们用下划线拼在一起就不是一回事了！但这时候最笨的办法还是很好用的：打开看起来包含某个东西的文件，“地毯式轰炸”，并将该过程重复下去。猜对了有奖，猜错了也不亏。

就这样我从`main`模块一路打到`cpu`模块，并且定位到这是`ui.c`中`cmd_info`的问题，只要加一个判断条件，就可以使得参数为`NULL`时不会异常退出。

而读框架代码产生了重要的副作用：我同时读了参数解析、`NEMU`各元件初始化、镜像装载等步骤，算是大体了解了`NEMU`的工作原理，从此它对于我来说不再是一个黑盒。更具里程碑意义的是，这次是我作为新手第一次克服恐惧，从`main`开始__RTFSC__，试图以掌握全局的眼光看待一个proj，而非像在程设实验里那样，仅仅满足于知道如何调用接口。

### 2-2 第一个BUG：`mov-cmp`

在这个用例中我需要自己实现第一条指令——`cmp`。我开始各种RTFSC、RTFM，半个小时后踉踉跄跄写出几行代码。输入`make test`，`i386 MANUAL`赫然于屏上，上面有着”需要实现下一个指令“的提示。我心满意足，把`objdump4nemu-i386`这串语句`alias`了一下，然后查看`mov-cmp`的汇编指令。

意想不到的事情出现了：程序之前停下时，`eip`并没有指向某个指令的开头！我下意识觉得是指令操作数长度出了问题，回去审查了一下宏和`opcode.c`，发现了`_v`和`_bv`的不同之处。原来我之前把`0x81`和`0x83`两个`group`下的指令都当`_v`来处理了。修改了一下函数名，`mov-cmp HIT GOOD TRAP`！

### 2-3 凶险的指针操作指令：`jmp`

实现第二条指令——`jmp`指令时出现了和2-2中类似的BUG。这时Windows自带的程序员计算器发挥了用处：我用预期的地址减去实际跳转到的地址，其差值正好是一条`jmp`指令的长度。怎么样，懂的都懂了吧？

### 2-4 完善基础设施：为栈操作指令DEBUG

`mov-c`出现怪BUG了：程序停止时其`eip`竟然指向`ELF`文件所在的内存区！我找来找去，猜测可能是某个指令试图读取`%ebp`时，读错了地方。我初步猜测是这条指令读取了`%ebp`附近的一个值。

为了验证这个猜想，我在`ui.h`中手动添加了一个扫描内存指令`x`，将`%ebp`附近的值打印出来，结果印证了我的想法。于是我精准定位到BUG所在位置，迅速将其排除。

### 2-5 在写了一百多个指令之后……

上午我只写了三条指令，但越做越快，下午写了一百多条。我发现了`opcode map`中前七个指令的相似之处，为它们编写了一套宏，放在`fundamental_cal.h`（这些函数以下简称`fc`函数）中，一次填好了63个指令；还填完了`jcc`等诸多指令。总计有一百多个，`NEMU`也顺带通过了若干测试用例。

这时`bubble_sort`给我当头一棒：`eip`又一次指向了`ELF`文件所在的内存区。

__但烦人的事来了，之前填过的指令里面，是哪条出现问题了呢？__

我细致评判了`jcc`，发现指令没有错位；为`fc`函数编写的宏也不应当有问题。在对自己写代码的水准一番考量后，我非常怀疑是栈操作指令和函数调用/返回指令出了问题。于是我开始深入DEBUG，使用二分法确定程序中断出现的位置：`si 100000`、`si 50000`、`si 75000`……最后在约第88282步左右确定：`leave`和`ret`中可能至少有一个有问题。

首先，`leave`时`%esp`被置为一个异常值，据此我找到对应函数中的问题；其次，`ret`的实现函数中`OPERAND`给出了一个错误的`val`，从而我对其细节进行修改。（细节忘了不少，就不再详说了）

程序中不见得只有这两个指令有问题，将其改好未必就能看到`HIT GOOD TRAP`。但这样做，只会离成功越来越近。

### 2-6 冷血杀手BUG：段错误

在我沉浸于通过`bubble-sort`的喜悦不到一秒之后，`quick_sort` 非常无情地抛出一个血红色的段错误。

这还有什么办法？又要调试了。仍利用二分法确定了程序中断位置（约第5150步），但查看寄存器信息时，我吃了一惊：诸多寄存器存储着混乱不堪的值，而`jmp`指令尽责地把`eip`设成寄存器里的值，程序在下一步执行时当然直接越界。

我推测可能是`fc`函数的实现出了问题。但到底是出了什么问题？我继续向之前执行过的指令推进，终于在第5000步左右，找到了寄存器值异常的根源：`add_i2rm_bv`没有为立即数做符号扩展，遂使寄存器产生了一个巨大的值，并不断传播下去。问题总算暴露了出来。

### 2-7 城府极深的BUG：`HIT BAD TRAP`

齐刷刷的一排`GOOD`中，`bit`赫然爆出一个`BAD`，扎眼无比。

看起来，程序对函数的调用都很正常，肯定只是在哪个地方运算时出了一点小问题，而且是极小的问题，仅仅是运算时多1少1的问题。但是，这个地方怎么找呢？

我尝试用2-6中溯源的办法来找，从`HIT BAD TRAP`的位置（第605步）向前不断推进，重复进行扫描内存、打印寄存器等工作，Windows程序员计算器也用上了，假定的异常根源从`%eax`变成`%edx`再变成栈底，关系越发复杂，到第400步左右实在难以为继。

这时，有朋友给我发消息说去外面吃烤肉。我欣然同意，立即起身收拾东西。我要享受美食，反正还有半个多月才交，国庆再也不想这可恶的BUG了。

然而吃烤肉的时候我就突然有灵感了：假如我结合二分和溯源的策略，比较它和参考实现中内存和寄存器值的差异，会怎样呢？这不就是简单的找不同嘛！

于是乎吃完饭我兴冲冲地赶回宿舍，迅速`copy`了一个参考实现的`NEMU`出来，同时打开两个终端，同时调试。半个小时不到结果就出来了：第295步的`shll %cl, %edx`出现问题，我的实现中`%edx`的值变为0，而标准实现中`%edx`保持为1，这一微小差异导致最后`HIT BAD TRAP`。

又是`fc`函数的问题！但是框架代码出错的概率远低于自己代码出错的概率，因而我回到了`alu.c`。将`%cl = 0, %edx = 1`代进函数一看，好家伙，里面有一步操作使得无符号0减去1，这不就产生最大的数了嘛？`%edx`左移一个巨大的数，肯定清零了呀！

> 在通过了所有的`alu_test_xxx()`之后，在本阶段调用`nemu/src/cpu/alu.c`中的函数实现相应的算术和逻辑运算指令并设置标志位寄存器时就有充足的信心了。

__光靠`alu_test_xxx()`，不行！__

__动手自己添加测试用例，彳亍！__

### 2-8 总结

DEBUG用到的东西：

- 完善调试器
- 灵活构造、运用宏来减少代码重复
- 设法捕捉BUG位置：二分、溯源、比较
- 趁手的工具：内存扫描、16进制计算器、`alias`、`MobaXterm`
- 合理推断，大胆假设小心求证
- 冷静的头脑

以上DEBUG的心路历程说起来还是很顺利，但实际操作过程中遇到了非常多的坑，这里只写下了DEBUG的手段和思路。不论是思考DEBUG的骚操作，还是DEBUG本身，都消耗了大量时间和精力，这在上面的描述中无法很好地体现出来。但重要的是，在PA 2-1中，我的__动手能力和思考能力__都得到了一定提升，这样想来，写代码二八开的时间分配也就值了。

